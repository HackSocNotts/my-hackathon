/**
 * active: is the flash currently being shown
 * show: make the flash active on next router push
 */
const insitalState = {
  active: false,
  show: false,
  title: '',
  message: '',
  type: '',
};

export default insitalState;
